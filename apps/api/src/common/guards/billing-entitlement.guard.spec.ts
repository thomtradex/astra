import { BillingEntitlementGuard } from './billing-entitlement.guard';

describe('BillingEntitlementGuard', () => {
  it('should instantiate', () => {
    expect(BillingEntitlementGuard).toBeDefined();
  });
});
